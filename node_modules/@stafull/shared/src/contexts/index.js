/**
 * StaFull Shared Contexts
 */

export { AuthProvider, useAuth, default as AuthContext } from './AuthContext';
export {
  SocketProvider,
  useSocket,
  useSocketEvent,
  useFranchiseSocket,
  useHQSocket,
  default as SocketContext,
} from './SocketContext';

// Re-export ProtectedRoute for convenience (lives in organisms but commonly used with auth)
export { ProtectedRoute } from '../components/organisms/ProtectedRoute';
